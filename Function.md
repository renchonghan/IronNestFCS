# IronNestFCS 功能详解 (v2.0)

> 目标游戏: Iron Nest: Heavy Turret Simulator (正式版), IL2CPP + MelonLoader, .NET 6.
> 本文面向玩家/使用者: 装了什么功能、怎么操作、HUD 怎么看。架构与实现见 [Architecture.md](Architecture.md)。

---

## 1. 项目定位

IronNestFCS 是自动化火控系统 (Fire Control System) Mod。玩家只需在沙盘上点选目标, Mod 自动完成: 弹道解算 → 采购/装填炮弹 → 调整炮塔方位与仰角 → 五步确认 → 解除保险 → 击发的全套流程, 支持动目标提前量 (TWS) 与双炮齐射。

**热重载**: 改代码后 build, 游戏内按 **F9** 即生效, 无需重启游戏。

---

## 2. 功能总览

| 功能 | 操作 | 说明 |
| --- | --- | --- |
| 一键打击 | 沙盘上**右键目标** | 自动入队, 解算/装填/瞄准/确认/击发全自动 |
| 任务队列 | 自动 | 多目标排队, 两炮并行, 队首乱序优化 |
| 齐射 | 右键已有目标 | 两炮同目标同时装弹、一击发 |
| 持续扫荡 | Numpad 0 | 新敌目标自动入队 (按优先级), 强制自动开火 |
| TWS 预瞄 | 沙盘按钮 TWS | 跟踪动目标速度, 火控算提前量; 关 = 直瞄目标位置 |
| 装药模式 | 沙盘按钮 T/N/X | Tight 最省 / Normal 默认效率 / Extra 强制最大 |
| 弹种选择 | 火控台右侧斜线按钮列 | 20 种弹 (AP/HE/APHE/DRIL/ATMC/...) |
| 落点指示器 | 自动 (开火后) | 红色弹道/弹头/杀伤圈/落地倒计时 |
| DUMP 退弹 | 自动 | 膛内弹不对/药不够时强制平射退弹 (无落点指示) |
| 蒸汽阀门 | Numpad + / - | 一键拧开/拧紧全部蒸汽阀门 |
| 自定义唱片机 | CustomRecords Mod | 扫描 UserData/CustomRecords 音频替换游戏唱片 |

---

## 3. 操作指南

### 3.1 目标点选 (右键)

- **右键地图实体** (敌我目标): 入队 (按当前选中弹种/装药模式); 已入队 → 升级齐射 (双圈标记); 已齐射/已在炮上 → 取消任务 (上炮任务只能取消, 不能改计划)。
- **令牌目标**: 拖 `MapToken_Artillery` 令牌上地图 → 右键令牌 = 虚拟目标入队 (带名称, 火控框跟令牌走, 目标跟着令牌移动); 令牌拖离地图自动撤任务。只有 T1-10 标记令牌可注册 (击杀/侦察/参考点令牌不注册)。
- **扫荡 (Numpad 0)**: Start 之后可用 — 新敌对目标自动按优先级入队 (FDC > 炮兵 > 装甲 > 其他), 并强制自动开火。

### 3.2 沙盘按钮列 (右侧第二列)

| 按钮 | 功能 |
| --- | --- |
| Auto Fire | 自动击发开关 (开 = 解保险后自动开火; 关 = 等玩家扣扳机) |
| Auto Task | 扫荡开关 (开 = 自动入队 + 强制 Auto Fire) |
| TWS | 动目标跟踪开关 (开 = 跟踪速度, 火控有预瞄: 炮的瞄准线/点式轨迹线/交汇点都出; 关 = 直瞄目标当前位置, 瞄准线直接连目标, 无轨迹线/交汇点) |
| Tight (T) / Normal (N) / Extra (X) | 装药模式三档 |

### 3.3 火控台右侧三按钮

| 按钮 | 状态 | 行为 |
| --- | --- | --- |
| Start | 绿 | 启动火控 (进 PreAiming; AutoFire 已开则 Semi-Auto); 顺带做一次乱序重排 |
| Pause | 黄 | 冻结派发与炮塔控制 (在途动作跑完; 飞行计时/落点指示不冻结) |
| Stop | 红 | 全停: 清队列/撤任务, 回 Manual |

### 3.4 四档运行模式

| 模式 | 进入 | 行为 |
| --- | --- | --- |
| Full-Auto | AutoTask 开 | 扫荡 + 强制自动开火 |
| Semi-Auto | AutoFire 开 | 自动到击发 |
| PreAiming | AutoFire 关 | 自动到解除保险, 等玩家扣扳机 |
| ManualSet | Stop / 启动默认 | 全手动, Mod 停手不碰硬件 |

### 3.5 装药模式三档

- **T (Tight)**: 最少装药 (最省; 动目标自动强制改 N — 目标移动距离变, 最少装药无裕量)。
- **N (Normal)**: 保证仰角 ≤30° 的尽量少装药 (默认)。
- **X (Extra)**: 强制 6 包最大装药 (极限射程/最大速度)。

---

## 4. HUD 面板 (左上角)

64 字符定宽面板, 荧光绿等宽字体:

```
 [IronNest FCS]  [Semi-Auto]        [CBC:---]        [10:02:03]
----------------------------------------------------------------
 [GUN-L] PHASE 3-1 TRAK | [ AP ] E:34.49 A:123.4 C:6 | FT:24.61
 [--:--:--] 123.4 12.34 | [ AP ] E:34.49 A:123.4 C:6 | T:-24.61
----------------------------------------------------------------
 [GUN-R] PHASE 1-1 WAIT | [NULL] E:--.-- A:---.- C:- | FT:--.--
 [--:--:--] ---.- --.-- | [----] E:--.-- A:---.- C:- | T:---.--
----------------------------------------------------------------
 Task Queue(3)                 |Finish Queue(2)
 [--:--:--] 042.0 04.20  AP  N |[10:02:15] 042.0 24.20 T:-12.31
 [--:--:--] 123.4 12.34  HE  X |[10:00:41] 123.4 12.34 T:---.--
 >>>[SALVO] 180.0 10.00  HE  X |
```

- **炮块第 1 行**: 相位 (装填/追踪进度) + 膛内弹 + 实际仰角/方位/装药 + 实际飞时 (FT:)。
- **炮块第 2 行**: 火控解算 (Mod 算的目标诸元) + 解算飞时倒计时 (T:-)。膛内弹 `[----]` = 膛空, `[NULL]` = 无数据。
- **Task Queue**: 待打任务 (左列, 8 行); 行首 [--:--:--] = 预定打击时间占位; `>>>[SALVO]` = 齐射对。
- **Finish Queue**: 已完成任务 (右列, 8 行, **最新的在最上面**); 行首 = 抵达时刻, T:- = 该发炮弹剩余飞行秒 (落地显示 --.--)。
- **CBC**: 反炮兵计时 (只读); 时钟 = 游戏任务时钟 (10:00:00 起)。

相位代号: 1-1 WAIT (等派发) / 1-2 SELC (采购) / 1-3 DUMP (退弹) / 2-1 SHRD (选弹) / 2-2 SHLD (推弹) / 2-3 PWDR (给药) / 2-4 LOAD (装填) / 3-1 TRAK (追踪) / 3-3 REST (复位) / 0-0 FALL (故障红显, 等玩家手动处理或换炮)。

---

## 5. 沙盘视觉元素

### 5.1 目标标记 (红)

- **菱形框**: 套在目标实体上 (选中/排队即出)。
- **编号 + 弹种标签**: 跟预瞄点走 (00T/01N/02X 米字数码; 在炮上 = L-N/R-X)。
- **红杀伤圈**: 该任务弹种的杀伤半径, 跟预瞄点走; 穿甲弹带 X 指示。
- **齐射框**: 齐射任务的第二圈菱形。

### 5.2 火控解算层 (绿, 开 TWS 才有预瞄)

- **炮的瞄准线** (A1): 炮塔 → 预瞄点 的绿虚线; 无预瞄时直连目标。
- **目标轨迹线** (A2): 目标 → 预瞄点 的**点式虚线** — 点疏密直观反映目标速度 (点越疏越快)。
- **最终轨迹线** (A3): 开火后细实线, 沿冻结轨迹随飞行缩短, 指示预测轨迹。
- **交汇点** (A4): 预瞄点绿实心点 = 弹道与目标轨迹的交汇预测点。

### 5.3 落点指示器 (红, 开火后)

- **红虚线**: 炮 → 落点 全长弹道 (保留到下一发开火)。
- **红实线 + 红点**: 未飞段 + 弹头, 随飞行推进; 落点下方计时数字。
- **红杀伤圈**: 落点处 (落地隐藏)。
- 落点 = 游戏自身解算的真实落点 (与弹坑一致), 与绿交汇点分离 = 火控误差可视。

### 5.4 其他

- **绿十字 + 瞄准圈**: 每炮实时瞄准指示 (装填完成即出, 含飞时数字)。
- **实体图标**: 菱形框 + 类型符号 (装甲方框/炮兵圆/FDC 六芒星/AA/参考点十字), 敌红/友蓝/参考点绿。

---

## 6. 常见情形

- **膛内弹不对/药不够**: Mod 自动平射退弹 (DUMP), HUD 炮行显示 `>>>>[DUMP]`, 无落点指示。
- **哑炮**: 自动重按保险+拉绳 3 次; DUMP 全哑 → 该炮挂起, 玩家手动处理 (Stop/Manual 复位)。
- **FALL 故障**: 装填动作失败/超时, 该炮停用等换策略; 玩家介入 (Stop) 复位。
- **令牌拖离地图**: 对应任务自动取消。
- **开火后切任务**: 上一发的飞行指示/落点倒计时照常走完 (不因新任务中断); 完成队列里每发各自倒计时, 落地定格 `--.--`。
- **动目标 + Tight 装药**: 入队时自动强制改 Normal (最少装药无裕量, 目标移动打不到新距离)。
- **玩家手动装填/介入**: 膛内状态显示实时跟随 (活读), Mod 派发会适配玩家手动装的弹 (弹对药够直接打)。
- **落点与预瞄点分离**: 绿交汇点 = Mod 解算的理论命中点, 红落点 = 游戏真实弹道落点 (弹坑一致) — 两者分离即火控误差, 属正常可视信息。
